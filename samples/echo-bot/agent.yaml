id: echo-bot
version: 0.1.0
description: |
  Minimal example agent — responds with a friendly greeting containing the word "hello".

model: opus

allow_edit:
  - prompt
  - skills

tool_allowlist:
  allow:
    - Read
    - Write
  deny:
    - Bash(*)

memory_seed: memory/MEMORY.md
